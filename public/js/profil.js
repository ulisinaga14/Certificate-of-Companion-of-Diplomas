var Tooltip = (function() {

    // Variables
  
    var $tooltip = $('[data-toggle="tooltip"]');
  
    function init() {
      $tooltip.tooltip();
    }
  
  
    // Events
    // Methods
  
    if ($tooltip.length) {
      init()
      f;
    }
  
  })();